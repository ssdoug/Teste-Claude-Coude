# TODO: implementar chamadas às APIs de LLM (Anthropic/OpenAI/etc) via `fetch`
# nativo do JS (módulo `js`), não pelos SDKs oficiais em Python — eles costumam
# usar transporte síncrono, incompatível com o runtime assíncrono do Pyodide.
